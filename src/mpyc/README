lancer dans deux terminaux à la racine du projet:

Pour lancer le serveur:
"python3 src/mpyc/server.py -M2 -I0"

Pour lancer le client:
"python3 src/mpyc/client.py -M2 -I1 {path}" ### avec path, le chemin relatif d'une image.
puis insérer un firstname, et un lastname ### La caste minuscule/majuscule est gérée, ainsi que les espaces.

Les résultats sont:
"Welcome {firstname} {lastname}" si l'utilisateur est reconnu
"You are not recognized, try again" si l'utilisateur n'est pas reconnu
